# Retrieve Derived memories instead of historical Private entries

The Pivot Guide's memory tool will return only user-scoped Derived memory context, selected Pivot data, outcomes, and identifiers; it will not return historical raw Private entries to the model. The current consented Check-in may be processed for the active request, while historical Private entries remain available only through user-controlled inspection and deletion.
