# Make memory user-governed

Unstuck will obtain clear consent before sending private entries to its configured model provider and will let a person process an entry without saving it, choose whether to save it, inspect and delete stored private entries and derived memories, and remove remembered patterns. The product's value depends on sensitive longitudinal memory, so user control is a core capability rather than a policy statement.
