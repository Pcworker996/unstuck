# Use Bedrock for language and embedding inference

Unstuck will use Amazon Bedrock to generate derived memories, semantic embeddings, and pivot recommendations. This gives the pivot guide managed model inference within the required AWS deployment while CockroachDB remains the persistent, user-controlled memory system of record.
