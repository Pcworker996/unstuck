# Use narrow HTTP routes around Run Pivot Protocol

The browser will use separate authenticated routes to run a Pivot Protocol, complete its outcome, inspect memories, delete a saved Check-in, and forget a pattern. These routes expose user-facing results only; embeddings, SQL, credentials, and historical raw Private entries remain inside the backend modules.
