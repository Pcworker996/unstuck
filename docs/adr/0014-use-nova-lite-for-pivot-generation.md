# Use Amazon Nova Lite for Pivot generation

The Pivot Guide will use Amazon Nova Lite through Bedrock's Converse API for short, low-temperature, structured recommendations. The model identifier remains deployment configuration so the runtime can select an available regional model without changing the Pivot Guide interface.
