# Expose only memory retrieval to the model

The Pivot Guide model will receive one constrained application tool, `retrieve_similar_memories`. The backend will hide account identity, SQL, persistence, deletion, forgetting, outcome recording, Derived-memory creation, and Pivot-library access behind application-owned operations.
