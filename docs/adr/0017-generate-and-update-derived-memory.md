# Generate and update Derived memory around the Pivot outcome

After consent, Bedrock will create a concise factual Derived-memory context and the backend will generate its embedding for the pending Check-in. Once the person records a selected Pivot and outcome, the backend will update the Derived context with those facts and regenerate its embedding so future retrieval reflects what actually happened.
