# Public identifiers for the Cognito User Pool created for Unstuck.
# These are identifiers, not AWS secrets. Do not commit an actual .env.local file.
NEXT_PUBLIC_COGNITO_USER_POOL_ID=us-east-1_example
NEXT_PUBLIC_COGNITO_USER_POOL_CLIENT_ID=exampleclientid
